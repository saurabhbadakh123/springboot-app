# Export cluster certificate
az aks get-credentials --resource-group pythonapp-rg --name pythonapp-aks --file cluster-cert.pem

# Trust the certificate (Windows)
Import-Certificate -FilePath .\cluster-cert.pem -CertStoreLocation Cert:\CurrentUser\Root
